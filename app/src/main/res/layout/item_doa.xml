<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="12dp"
    app:cardCornerRadius="18dp"
    app:cardElevation="6dp"
    app:cardBackgroundColor="@color/cream">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Judul Doa -->
        <TextView
            android:id="@+id/tvJudul"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Masuk Rumah"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/green_islamic"/>

        <!-- Teks Arab -->
        <TextView
            android:id="@+id/tvArab"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="بِسْمِ اللَّهِ وَلَجْنَا..."
            android:textSize="22sp"
            android:textColor="@color/dark_text"
            android:gravity="end"
            android:lineSpacingExtra="6dp"/>

        <!-- Latin -->
        <TextView
            android:id="@+id/tvLatin"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Bismillahi walajnaa..."
            android:textStyle="italic"
            android:textColor="@color/dark_text"/>

        <!-- Arti -->
        <TextView
            android:id="@+id/tvArti"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Dengan nama Allah kami masuk..."
            android:textColor="@color/dark_text"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>
